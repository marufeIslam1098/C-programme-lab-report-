#include <stdio.h>
#include <time.h>
#include <unistd.h>

int main() {
    while (1) {
        time_t t = time(NULL);
        printf("\r%s", ctime(&t));
        fflush(stdout);
        sleep(1);
    }
}
