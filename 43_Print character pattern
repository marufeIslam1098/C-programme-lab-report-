#include <stdio.h>

int main(){
    int n; printf("Enter rows: "); scanf("%d",&n);
    for (int i=0;i<n;i++){
        for (int j=0;j<=i;j++) putchar('A' + i);
        putchar('\n');
    }
    return 0;
}
