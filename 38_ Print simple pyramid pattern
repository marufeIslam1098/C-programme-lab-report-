#include <stdio.h>

int main() {
    int n;
    printf("Enter rows: "); scanf("%d",&n);
    for (int i=1;i<=n;i++){
        for (int s=0;s<n-i;s++) putchar(' ');
        for (int k=0;k<2*i-1;k++) putchar('*');
        putchar('\n');
    }
    return 0;
}
